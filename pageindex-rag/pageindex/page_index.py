from .utils import *

def page_index(doc, model=None, **kwargs):
    # Stub for PDF indexing if needed
    print("PDF indexing not fully implemented in this SHIM.")
    return {"doc_name": str(doc), "structure": []}
